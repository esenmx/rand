name: rand
description: "Advanced, easy-to-use random generator for Dart with simple syntax. Ideal for efficient randomness in code."
version: 1.0.3
homepage: https://mehmetesen.com
repository: https://github.com/esenmx/rand

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.10.0

dev_dependencies:
  lint: ^2.3.0
  test: ^1.25.1
